{
  "name": "Ubuntu + Java + Maven + Node + Angular",
  "dockerFile": "Dockerfile",
  "appPort": [8080, 4200, 22],
  "forwardPorts": [8080, 4200, 22],
  "settings": {
    "terminal.integrated.defaultProfile.linux": "bash"
  },
  "postCreateCommand": "service ssh start && node --version && ng version && mvn --version",
  "extensions": [
    "vscjava.vscode-java-pack",
    "ms-azuretools.vscode-docker",
    "angular.ng-template"
  ]
}